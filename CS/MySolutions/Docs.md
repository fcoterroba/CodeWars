# C# docs

## Linq

Linq is a preinstalled C# "library" which allows make SQL-similar queries. Example
```cs
// This returns the number of trues in the array
sheeps = new bool[] { true, false, true, true, false, true, true, false, true };
return sheeps.Count(s => s);
```
