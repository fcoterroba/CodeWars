# C++ Docs

## Variables inizitialization

To create variables you can use `std::string result;` to create an empty string

## Vectores

Vector is array-like specific from C++.

### Loop over it

To loop over a fector you just need to `for(size_t i =0; i < words.size();++i){ / ... }`
